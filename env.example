# Example environment configuration for NeuroSphere UI
#
# Copy this file to `.env.local` or define these variables in your
# deployment platform (e.g. Vercel, Netlify).  Vite exposes only
# variables prefixed with `VITE_` to the client code.

# Base URL for the backend orchestrator.  This value should include
# the hostname and optional port, but MUST NOT include the `/api` path.
# The API client will automatically append `/api` if missing.
#
# Example values:
#   http://localhost:5000
#   https://app.neurospherevoiceai.com

VITE_API_URL=http://localhost:5000